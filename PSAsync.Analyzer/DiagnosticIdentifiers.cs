namespace PSAsync.Analyzer
{
    public static class DiagnosticIdentifiers
    {
        public const string PSASYNC001 = nameof(PSASYNC001);

        public const string PSASYNC002 = nameof(PSASYNC002);
    }
}
